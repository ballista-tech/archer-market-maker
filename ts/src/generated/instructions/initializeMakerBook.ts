/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_MAKER_BOOK_DISCRIMINATOR = 6;

export function getInitializeMakerBookDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(INITIALIZE_MAKER_BOOK_DISCRIMINATOR);
}

export type InitializeMakerBookInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMakerAccount extends string | AccountMeta<string> = string,
  TAccountMakerBookAccount extends string | AccountMeta<string> = string,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMakerAccount extends string
        ? WritableSignerAccount<TAccountMakerAccount> &
            AccountSignerMeta<TAccountMakerAccount>
        : TAccountMakerAccount,
      TAccountMakerBookAccount extends string
        ? WritableAccount<TAccountMakerBookAccount>
        : TAccountMakerBookAccount,
      TAccountMarketAccount extends string
        ? ReadonlyAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeMakerBookInstructionData = {
  discriminator: number;
  kind: number;
};

export type InitializeMakerBookInstructionDataArgs = { kind: number };

export function getInitializeMakerBookInstructionDataEncoder(): FixedSizeEncoder<InitializeMakerBookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["kind", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_MAKER_BOOK_DISCRIMINATOR,
    }),
  );
}

export function getInitializeMakerBookInstructionDataDecoder(): FixedSizeDecoder<InitializeMakerBookInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["kind", getU8Decoder()],
  ]);
}

export function getInitializeMakerBookInstructionDataCodec(): FixedSizeCodec<
  InitializeMakerBookInstructionDataArgs,
  InitializeMakerBookInstructionData
> {
  return combineCodec(
    getInitializeMakerBookInstructionDataEncoder(),
    getInitializeMakerBookInstructionDataDecoder(),
  );
}

export type InitializeMakerBookInput<
  TAccountMakerAccount extends string = string,
  TAccountMakerBookAccount extends string = string,
  TAccountMarketAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Market maker pubkey */
  makerAccount: TransactionSigner<TAccountMakerAccount>;
  /** MakerBook PDA for market maker and corresponding market */
  makerBookAccount: Address<TAccountMakerBookAccount>;
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  kind: InitializeMakerBookInstructionDataArgs["kind"];
};

export function getInitializeMakerBookInstruction<
  TAccountMakerAccount extends string,
  TAccountMakerBookAccount extends string,
  TAccountMarketAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: InitializeMakerBookInput<
    TAccountMakerAccount,
    TAccountMakerBookAccount,
    TAccountMarketAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeMakerBookInstruction<
  TProgramAddress,
  TAccountMakerAccount,
  TAccountMakerBookAccount,
  TAccountMarketAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    makerAccount: { value: input.makerAccount ?? null, isWritable: true },
    makerBookAccount: {
      value: input.makerBookAccount ?? null,
      isWritable: true,
    },
    marketAccount: { value: input.marketAccount ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("makerAccount", accounts.makerAccount),
      getAccountMeta("makerBookAccount", accounts.makerBookAccount),
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeMakerBookInstructionDataEncoder().encode(
      args as InitializeMakerBookInstructionDataArgs,
    ),
    programAddress,
  } as InitializeMakerBookInstruction<
    TProgramAddress,
    TAccountMakerAccount,
    TAccountMakerBookAccount,
    TAccountMarketAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeMakerBookInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market maker pubkey */
    makerAccount: TAccountMetas[0];
    /** MakerBook PDA for market maker and corresponding market */
    makerBookAccount: TAccountMetas[1];
    /** Market state account */
    marketAccount: TAccountMetas[2];
    /** System program */
    systemProgram: TAccountMetas[3];
  };
  data: InitializeMakerBookInstructionData;
};

export function parseInitializeMakerBookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeMakerBookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      makerAccount: getNextAccount(),
      makerBookAccount: getNextAccount(),
      marketAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeMakerBookInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
