/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_MAKER_BOOK_DISCRIMINATOR = 31;

export function getCloseMakerBookDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CLOSE_MAKER_BOOK_DISCRIMINATOR);
}

export type CloseMakerBookInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMakerAccount extends string | AccountMeta<string> = string,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountMakerBookAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMakerAccount extends string
        ? WritableSignerAccount<TAccountMakerAccount> &
            AccountSignerMeta<TAccountMakerAccount>
        : TAccountMakerAccount,
      TAccountMarketAccount extends string
        ? ReadonlyAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountMakerBookAccount extends string
        ? WritableAccount<TAccountMakerBookAccount>
        : TAccountMakerBookAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CloseMakerBookInstructionData = { discriminator: number };

export type CloseMakerBookInstructionDataArgs = {};

export function getCloseMakerBookInstructionDataEncoder(): FixedSizeEncoder<CloseMakerBookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CLOSE_MAKER_BOOK_DISCRIMINATOR }),
  );
}

export function getCloseMakerBookInstructionDataDecoder(): FixedSizeDecoder<CloseMakerBookInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCloseMakerBookInstructionDataCodec(): FixedSizeCodec<
  CloseMakerBookInstructionDataArgs,
  CloseMakerBookInstructionData
> {
  return combineCodec(
    getCloseMakerBookInstructionDataEncoder(),
    getCloseMakerBookInstructionDataDecoder(),
  );
}

export type CloseMakerBookInput<
  TAccountMakerAccount extends string = string,
  TAccountMarketAccount extends string = string,
  TAccountMakerBookAccount extends string = string,
> = {
  /** Book owner; receives the rent refund */
  makerAccount: TransactionSigner<TAccountMakerAccount>;
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  /** MakerBook PDA to close */
  makerBookAccount: Address<TAccountMakerBookAccount>;
};

export function getCloseMakerBookInstruction<
  TAccountMakerAccount extends string,
  TAccountMarketAccount extends string,
  TAccountMakerBookAccount extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: CloseMakerBookInput<
    TAccountMakerAccount,
    TAccountMarketAccount,
    TAccountMakerBookAccount
  >,
  config?: { programAddress?: TProgramAddress },
): CloseMakerBookInstruction<
  TProgramAddress,
  TAccountMakerAccount,
  TAccountMarketAccount,
  TAccountMakerBookAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    makerAccount: { value: input.makerAccount ?? null, isWritable: true },
    marketAccount: { value: input.marketAccount ?? null, isWritable: false },
    makerBookAccount: {
      value: input.makerBookAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("makerAccount", accounts.makerAccount),
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("makerBookAccount", accounts.makerBookAccount),
    ],
    data: getCloseMakerBookInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseMakerBookInstruction<
    TProgramAddress,
    TAccountMakerAccount,
    TAccountMarketAccount,
    TAccountMakerBookAccount
  >);
}

export type ParsedCloseMakerBookInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Book owner; receives the rent refund */
    makerAccount: TAccountMetas[0];
    /** Market state account */
    marketAccount: TAccountMetas[1];
    /** MakerBook PDA to close */
    makerBookAccount: TAccountMetas[2];
  };
  data: CloseMakerBookInstructionData;
};

export function parseCloseMakerBookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseMakerBookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      makerAccount: getNextAccount(),
      marketAccount: getNextAccount(),
      makerBookAccount: getNextAccount(),
    },
    data: getCloseMakerBookInstructionDataDecoder().decode(instruction.data),
  };
}
