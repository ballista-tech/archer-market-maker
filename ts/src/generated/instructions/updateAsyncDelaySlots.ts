/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ASYNC_DELAY_SLOTS_DISCRIMINATOR = 2;

export function getUpdateAsyncDelaySlotsDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(UPDATE_ASYNC_DELAY_SLOTS_DISCRIMINATOR);
}

export type UpdateAsyncDelaySlotsInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountAdminAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarketAccount extends string
        ? WritableAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountAdminAccount extends string
        ? ReadonlySignerAccount<TAccountAdminAccount> &
            AccountSignerMeta<TAccountAdminAccount>
        : TAccountAdminAccount,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAsyncDelaySlotsInstructionData = {
  discriminator: number;
  minAsyncDelaySlots: number;
  maxAsyncDelaySlots: number;
};

export type UpdateAsyncDelaySlotsInstructionDataArgs = {
  minAsyncDelaySlots: number;
  maxAsyncDelaySlots: number;
};

export function getUpdateAsyncDelaySlotsInstructionDataEncoder(): FixedSizeEncoder<UpdateAsyncDelaySlotsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["minAsyncDelaySlots", getU16Encoder()],
      ["maxAsyncDelaySlots", getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_ASYNC_DELAY_SLOTS_DISCRIMINATOR,
    }),
  );
}

export function getUpdateAsyncDelaySlotsInstructionDataDecoder(): FixedSizeDecoder<UpdateAsyncDelaySlotsInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["minAsyncDelaySlots", getU16Decoder()],
    ["maxAsyncDelaySlots", getU16Decoder()],
  ]);
}

export function getUpdateAsyncDelaySlotsInstructionDataCodec(): FixedSizeCodec<
  UpdateAsyncDelaySlotsInstructionDataArgs,
  UpdateAsyncDelaySlotsInstructionData
> {
  return combineCodec(
    getUpdateAsyncDelaySlotsInstructionDataEncoder(),
    getUpdateAsyncDelaySlotsInstructionDataDecoder(),
  );
}

export type UpdateAsyncDelaySlotsInput<
  TAccountMarketAccount extends string = string,
  TAccountAdminAccount extends string = string,
> = {
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  /** Market admin authority */
  adminAccount: TransactionSigner<TAccountAdminAccount>;
  minAsyncDelaySlots: UpdateAsyncDelaySlotsInstructionDataArgs["minAsyncDelaySlots"];
  maxAsyncDelaySlots: UpdateAsyncDelaySlotsInstructionDataArgs["maxAsyncDelaySlots"];
};

export function getUpdateAsyncDelaySlotsInstruction<
  TAccountMarketAccount extends string,
  TAccountAdminAccount extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: UpdateAsyncDelaySlotsInput<
    TAccountMarketAccount,
    TAccountAdminAccount
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateAsyncDelaySlotsInstruction<
  TProgramAddress,
  TAccountMarketAccount,
  TAccountAdminAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    marketAccount: { value: input.marketAccount ?? null, isWritable: true },
    adminAccount: { value: input.adminAccount ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("adminAccount", accounts.adminAccount),
    ],
    data: getUpdateAsyncDelaySlotsInstructionDataEncoder().encode(
      args as UpdateAsyncDelaySlotsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAsyncDelaySlotsInstruction<
    TProgramAddress,
    TAccountMarketAccount,
    TAccountAdminAccount
  >);
}

export type ParsedUpdateAsyncDelaySlotsInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market state account */
    marketAccount: TAccountMetas[0];
    /** Market admin authority */
    adminAccount: TAccountMetas[1];
  };
  data: UpdateAsyncDelaySlotsInstructionData;
};

export function parseUpdateAsyncDelaySlotsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAsyncDelaySlotsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      marketAccount: getNextAccount(),
      adminAccount: getNextAccount(),
    },
    data: getUpdateAsyncDelaySlotsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
