/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const TRANSFER_ADMIN_DISCRIMINATOR = 5;

export function getTransferAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(TRANSFER_ADMIN_DISCRIMINATOR);
}

export type TransferAdminInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountCurrentAdminAccount extends string | AccountMeta<string> = string,
  TAccountNewAdminAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarketAccount extends string
        ? WritableAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountCurrentAdminAccount extends string
        ? ReadonlySignerAccount<TAccountCurrentAdminAccount> &
            AccountSignerMeta<TAccountCurrentAdminAccount>
        : TAccountCurrentAdminAccount,
      TAccountNewAdminAccount extends string
        ? ReadonlyAccount<TAccountNewAdminAccount>
        : TAccountNewAdminAccount,
      ...TRemainingAccounts,
    ]
  >;

export type TransferAdminInstructionData = { discriminator: number };

export type TransferAdminInstructionDataArgs = {};

export function getTransferAdminInstructionDataEncoder(): FixedSizeEncoder<TransferAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: TRANSFER_ADMIN_DISCRIMINATOR }),
  );
}

export function getTransferAdminInstructionDataDecoder(): FixedSizeDecoder<TransferAdminInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getTransferAdminInstructionDataCodec(): FixedSizeCodec<
  TransferAdminInstructionDataArgs,
  TransferAdminInstructionData
> {
  return combineCodec(
    getTransferAdminInstructionDataEncoder(),
    getTransferAdminInstructionDataDecoder(),
  );
}

export type TransferAdminInput<
  TAccountMarketAccount extends string = string,
  TAccountCurrentAdminAccount extends string = string,
  TAccountNewAdminAccount extends string = string,
> = {
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  /** Current market admin */
  currentAdminAccount: TransactionSigner<TAccountCurrentAdminAccount>;
  /** New admin pubkey */
  newAdminAccount: Address<TAccountNewAdminAccount>;
};

export function getTransferAdminInstruction<
  TAccountMarketAccount extends string,
  TAccountCurrentAdminAccount extends string,
  TAccountNewAdminAccount extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: TransferAdminInput<
    TAccountMarketAccount,
    TAccountCurrentAdminAccount,
    TAccountNewAdminAccount
  >,
  config?: { programAddress?: TProgramAddress },
): TransferAdminInstruction<
  TProgramAddress,
  TAccountMarketAccount,
  TAccountCurrentAdminAccount,
  TAccountNewAdminAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    marketAccount: { value: input.marketAccount ?? null, isWritable: true },
    currentAdminAccount: {
      value: input.currentAdminAccount ?? null,
      isWritable: false,
    },
    newAdminAccount: {
      value: input.newAdminAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("currentAdminAccount", accounts.currentAdminAccount),
      getAccountMeta("newAdminAccount", accounts.newAdminAccount),
    ],
    data: getTransferAdminInstructionDataEncoder().encode({}),
    programAddress,
  } as TransferAdminInstruction<
    TProgramAddress,
    TAccountMarketAccount,
    TAccountCurrentAdminAccount,
    TAccountNewAdminAccount
  >);
}

export type ParsedTransferAdminInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market state account */
    marketAccount: TAccountMetas[0];
    /** Current market admin */
    currentAdminAccount: TAccountMetas[1];
    /** New admin pubkey */
    newAdminAccount: TAccountMetas[2];
  };
  data: TransferAdminInstructionData;
};

export function parseTransferAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTransferAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      marketAccount: getNextAccount(),
      currentAdminAccount: getNextAccount(),
      newAdminAccount: getNextAccount(),
    },
    data: getTransferAdminInstructionDataDecoder().decode(instruction.data),
  };
}
