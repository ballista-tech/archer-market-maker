/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_SYNC_SPREAD_DISCRIMINATOR = 26;

export function getUpdateSyncSpreadDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(UPDATE_SYNC_SPREAD_DISCRIMINATOR);
}

export type UpdateSyncSpreadInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMakerAccount extends string | AccountMeta<string> = string,
  TAccountMakerBookAccount extends string | AccountMeta<string> = string,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMakerAccount extends string
        ? ReadonlySignerAccount<TAccountMakerAccount> &
            AccountSignerMeta<TAccountMakerAccount>
        : TAccountMakerAccount,
      TAccountMakerBookAccount extends string
        ? WritableAccount<TAccountMakerBookAccount>
        : TAccountMakerBookAccount,
      TAccountMarketAccount extends string
        ? ReadonlyAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateSyncSpreadInstructionData = {
  discriminator: number;
  syncSpreadTicks: number;
};

export type UpdateSyncSpreadInstructionDataArgs = { syncSpreadTicks: number };

export function getUpdateSyncSpreadInstructionDataEncoder(): FixedSizeEncoder<UpdateSyncSpreadInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["syncSpreadTicks", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_SYNC_SPREAD_DISCRIMINATOR }),
  );
}

export function getUpdateSyncSpreadInstructionDataDecoder(): FixedSizeDecoder<UpdateSyncSpreadInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["syncSpreadTicks", getU16Decoder()],
  ]);
}

export function getUpdateSyncSpreadInstructionDataCodec(): FixedSizeCodec<
  UpdateSyncSpreadInstructionDataArgs,
  UpdateSyncSpreadInstructionData
> {
  return combineCodec(
    getUpdateSyncSpreadInstructionDataEncoder(),
    getUpdateSyncSpreadInstructionDataDecoder(),
  );
}

export type UpdateSyncSpreadInput<
  TAccountMakerAccount extends string = string,
  TAccountMakerBookAccount extends string = string,
  TAccountMarketAccount extends string = string,
> = {
  /** Maker (book owner or delegate) */
  makerAccount: TransactionSigner<TAccountMakerAccount>;
  /** MakerBook PDA */
  makerBookAccount: Address<TAccountMakerBookAccount>;
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  syncSpreadTicks: UpdateSyncSpreadInstructionDataArgs["syncSpreadTicks"];
};

export function getUpdateSyncSpreadInstruction<
  TAccountMakerAccount extends string,
  TAccountMakerBookAccount extends string,
  TAccountMarketAccount extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: UpdateSyncSpreadInput<
    TAccountMakerAccount,
    TAccountMakerBookAccount,
    TAccountMarketAccount
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateSyncSpreadInstruction<
  TProgramAddress,
  TAccountMakerAccount,
  TAccountMakerBookAccount,
  TAccountMarketAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    makerAccount: { value: input.makerAccount ?? null, isWritable: false },
    makerBookAccount: {
      value: input.makerBookAccount ?? null,
      isWritable: true,
    },
    marketAccount: { value: input.marketAccount ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("makerAccount", accounts.makerAccount),
      getAccountMeta("makerBookAccount", accounts.makerBookAccount),
      getAccountMeta("marketAccount", accounts.marketAccount),
    ],
    data: getUpdateSyncSpreadInstructionDataEncoder().encode(
      args as UpdateSyncSpreadInstructionDataArgs,
    ),
    programAddress,
  } as UpdateSyncSpreadInstruction<
    TProgramAddress,
    TAccountMakerAccount,
    TAccountMakerBookAccount,
    TAccountMarketAccount
  >);
}

export type ParsedUpdateSyncSpreadInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Maker (book owner or delegate) */
    makerAccount: TAccountMetas[0];
    /** MakerBook PDA */
    makerBookAccount: TAccountMetas[1];
    /** Market state account */
    marketAccount: TAccountMetas[2];
  };
  data: UpdateSyncSpreadInstructionData;
};

export function parseUpdateSyncSpreadInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateSyncSpreadInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      makerAccount: getNextAccount(),
      makerBookAccount: getNextAccount(),
      marketAccount: getNextAccount(),
    },
    data: getUpdateSyncSpreadInstructionDataDecoder().decode(instruction.data),
  };
}
