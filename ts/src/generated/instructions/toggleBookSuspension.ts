/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCHER_V1_PROGRAM_ADDRESS } from "../programs";

export const TOGGLE_BOOK_SUSPENSION_DISCRIMINATOR = 13;

export function getToggleBookSuspensionDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(TOGGLE_BOOK_SUSPENSION_DISCRIMINATOR);
}

export type ToggleBookSuspensionInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountAdminAccount extends string | AccountMeta<string> = string,
  TAccountMakerBookAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarketAccount extends string
        ? WritableAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountAdminAccount extends string
        ? ReadonlySignerAccount<TAccountAdminAccount> &
            AccountSignerMeta<TAccountAdminAccount>
        : TAccountAdminAccount,
      TAccountMakerBookAccount extends string
        ? WritableAccount<TAccountMakerBookAccount>
        : TAccountMakerBookAccount,
      ...TRemainingAccounts,
    ]
  >;

export type ToggleBookSuspensionInstructionData = { discriminator: number };

export type ToggleBookSuspensionInstructionDataArgs = {};

export function getToggleBookSuspensionInstructionDataEncoder(): FixedSizeEncoder<ToggleBookSuspensionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: TOGGLE_BOOK_SUSPENSION_DISCRIMINATOR,
    }),
  );
}

export function getToggleBookSuspensionInstructionDataDecoder(): FixedSizeDecoder<ToggleBookSuspensionInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getToggleBookSuspensionInstructionDataCodec(): FixedSizeCodec<
  ToggleBookSuspensionInstructionDataArgs,
  ToggleBookSuspensionInstructionData
> {
  return combineCodec(
    getToggleBookSuspensionInstructionDataEncoder(),
    getToggleBookSuspensionInstructionDataDecoder(),
  );
}

export type ToggleBookSuspensionInput<
  TAccountMarketAccount extends string = string,
  TAccountAdminAccount extends string = string,
  TAccountMakerBookAccount extends string = string,
> = {
  /** Market state account */
  marketAccount: Address<TAccountMarketAccount>;
  /** Market admin authority */
  adminAccount: TransactionSigner<TAccountAdminAccount>;
  /** MakerBook PDA for market maker */
  makerBookAccount: Address<TAccountMakerBookAccount>;
};

export function getToggleBookSuspensionInstruction<
  TAccountMarketAccount extends string,
  TAccountAdminAccount extends string,
  TAccountMakerBookAccount extends string,
  TProgramAddress extends Address = typeof ARCHER_V1_PROGRAM_ADDRESS,
>(
  input: ToggleBookSuspensionInput<
    TAccountMarketAccount,
    TAccountAdminAccount,
    TAccountMakerBookAccount
  >,
  config?: { programAddress?: TProgramAddress },
): ToggleBookSuspensionInstruction<
  TProgramAddress,
  TAccountMarketAccount,
  TAccountAdminAccount,
  TAccountMakerBookAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARCHER_V1_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    marketAccount: { value: input.marketAccount ?? null, isWritable: true },
    adminAccount: { value: input.adminAccount ?? null, isWritable: false },
    makerBookAccount: {
      value: input.makerBookAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("adminAccount", accounts.adminAccount),
      getAccountMeta("makerBookAccount", accounts.makerBookAccount),
    ],
    data: getToggleBookSuspensionInstructionDataEncoder().encode({}),
    programAddress,
  } as ToggleBookSuspensionInstruction<
    TProgramAddress,
    TAccountMarketAccount,
    TAccountAdminAccount,
    TAccountMakerBookAccount
  >);
}

export type ParsedToggleBookSuspensionInstruction<
  TProgram extends string = typeof ARCHER_V1_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market state account */
    marketAccount: TAccountMetas[0];
    /** Market admin authority */
    adminAccount: TAccountMetas[1];
    /** MakerBook PDA for market maker */
    makerBookAccount: TAccountMetas[2];
  };
  data: ToggleBookSuspensionInstructionData;
};

export function parseToggleBookSuspensionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedToggleBookSuspensionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      marketAccount: getNextAccount(),
      adminAccount: getNextAccount(),
      makerBookAccount: getNextAccount(),
    },
    data: getToggleBookSuspensionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
